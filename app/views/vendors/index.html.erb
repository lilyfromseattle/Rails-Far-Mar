<div class = "view-new">
<h2 class = "page-title">All Vendors</h2>
<div class = "each-do">
<% @vendors.each do |vendors| %>
    <div class = "all">
      <tr>
        <td class = "table-data"><a href="#" class = "name"><%= vendor.name %></a></td>
        <td><%= link_to "x", "/vendors/destroy/#{vendors.id}", class: "destroy" %></td>
        <!-- <td><%= product.name %></td> -->
        <!-- <td><%= link_to "Destroy", "/products/destroy/#{product.id}" %></td> -->
        <!-- <td><%= link_to "Edit", "/products/#{product.id}/edit" %></td> -->
      </tr>
  </div>
    <div class = "product-description">

    </div>
  <% end %>
</div>

<%= link_to "New Vendor", "/vendors/new", class: "links" %>
</div>
